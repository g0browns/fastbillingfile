services:
  shiftnotes:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastshiftnotes-app
    env_file:
      - .env
    ports:
      - "8006:8001"
    restart: unless-stopped
    volumes:
      - ./output:/app/output
